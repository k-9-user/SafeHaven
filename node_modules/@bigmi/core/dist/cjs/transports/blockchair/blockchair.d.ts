import type { HttpTransportConfig } from '../http.js';
type BlockchairConfig = {
    baseUrl?: string;
    apiKey?: string;
} & HttpTransportConfig;
export declare const blockchair: (config?: BlockchairConfig) => import("../http.js").HttpTransport;
export {};
