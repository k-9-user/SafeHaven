Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const require_errors_connectors = require("../errors/connectors.js");
const require_factories_createConnector = require("../factories/createConnector.js");
const require_utils_createBidirectionalMap = require("../utils/createBidirectionalMap.js");
let _bigmi_core = require("@bigmi/core");
//#region src/connectors/bitget.ts
function bitget(parameters = {}) {
	const { forward: BitgetBitcoinNetworkChainIdMap, reverse: ChainIdToBitgetMap } = require_utils_createBidirectionalMap.createBidirectionalMap([
		["livenet", _bigmi_core.ChainId.BITCOIN_MAINNET],
		["testnet", _bigmi_core.ChainId.BITCOIN_TESTNET],
		["signet", _bigmi_core.ChainId.BITCOIN_SIGNET]
	]);
	const { shimDisconnect = true } = parameters;
	let accountsChanged;
	let chainChanged;
	return require_factories_createConnector.createConnector((config) => ({
		id: "bitget",
		name: "Bitget",
		type: bitget.type,
		icon: "data:image/svg+xml;base64,",
		async setup() {},
		async getInternalProvider() {
			if (typeof window === "undefined") return;
			if ("bitkeep" in window) return window.bitkeep?.unisat;
		},
		async getProvider() {
			const internalProvider = await this.getInternalProvider();
			if (!internalProvider) return;
			return { request: this.request.bind(internalProvider) };
		},
		async request({ method, params }) {
			switch (method) {
				case "signPsbt": {
					const { psbt, ...options } = params;
					const toSignInputs = options.inputsToSign.flatMap(({ sigHash, address, signingIndexes }) => signingIndexes.map((index) => ({
						index,
						address,
						sighashTypes: sigHash !== void 0 ? [sigHash] : void 0
					})));
					return await this.signPsbt(psbt, {
						toSignInputs,
						autoFinalized: options.finalize
					});
				}
				default: throw new _bigmi_core.MethodNotSupportedRpcError();
			}
		},
		async connect() {
			const provider = await this.getInternalProvider();
			if (!provider) throw new _bigmi_core.ProviderNotFoundError();
			try {
				if (!await provider.requestAccounts()) throw new _bigmi_core.BaseError("error connecting to your wallet");
				const accounts = await this.getAccounts();
				const chainId = await this.getChainId();
				if (!accountsChanged) {
					accountsChanged = this.onAccountsChanged.bind(this);
					provider.addListener("accountsChanged", accountsChanged);
				}
				if (!chainChanged) {
					chainChanged = (network) => this.onChainChanged(BitgetBitcoinNetworkChainIdMap[network]);
					provider.addListener("networkChanged", chainChanged);
				}
				if (shimDisconnect) await Promise.all([config.storage?.setItem(`${this.id}.connected`, true), config.storage?.removeItem(`${this.id}.disconnected`)]);
				return {
					accounts,
					chainId
				};
			} catch (error) {
				await this.disconnect();
				throw new _bigmi_core.UserRejectedRequestError(error.message);
			}
		},
		async disconnect() {
			const provider = await this.getInternalProvider();
			if (accountsChanged) {
				provider?.removeListener("accountsChanged", accountsChanged);
				accountsChanged = void 0;
			}
			if (chainChanged) {
				provider?.removeListener("networkChanged", chainChanged);
				chainChanged = void 0;
			}
			if (shimDisconnect) await Promise.all([config.storage?.setItem(`${this.id}.disconnected`, true), config.storage?.removeItem(`${this.id}.connected`)]);
		},
		async getAccounts() {
			const provider = await this.getInternalProvider();
			if (!provider) throw new _bigmi_core.ProviderNotFoundError();
			const address = (await provider.getAccounts())[0];
			const publicKey = await provider.getPublicKey();
			if (!publicKey.length) throw new _bigmi_core.BaseError("public key not found");
			const { type, purpose } = (0, _bigmi_core.getAddressInfo)(address);
			return [{
				address,
				addressType: type,
				publicKey,
				purpose
			}];
		},
		async getChainId() {
			const provider = await this.getInternalProvider();
			if (!provider) throw new _bigmi_core.ProviderNotFoundError();
			return BitgetBitcoinNetworkChainIdMap[await provider.getNetwork()];
		},
		async isAuthorized() {
			try {
				if (shimDisconnect) return Boolean(await config.storage?.getItem(`${this.id}.connected`));
				return !!(await this.getAccounts()).length;
			} catch {
				return false;
			}
		},
		async switchChain({ chainId }) {
			try {
				const provider = await this.getInternalProvider();
				if (!provider) throw new _bigmi_core.ProviderNotFoundError();
				const network = ChainIdToBitgetMap[chainId];
				if (!network) throw new require_errors_connectors.ChainNotSupportedError(chainId, bitget.name);
				const result = await provider.switchNetwork(network);
				return Boolean(result);
			} catch {
				return false;
			}
		},
		async onAccountsChanged(accounts) {
			if (accounts.length === 0) this.onDisconnect();
			else {
				if (!await this.getInternalProvider()) throw new _bigmi_core.ProviderNotFoundError();
				const newAccounts = await this.getAccounts();
				config.emitter.emit("change", { accounts: newAccounts });
			}
		},
		onChainChanged(chainId) {
			config.emitter.emit("change", { chainId });
		},
		async onDisconnect(_error) {
			config.emitter.emit("disconnect");
		}
	}));
}
bitget.type = "UTXO";
//#endregion
exports.bitget = bitget;

//# sourceMappingURL=bitget.js.map