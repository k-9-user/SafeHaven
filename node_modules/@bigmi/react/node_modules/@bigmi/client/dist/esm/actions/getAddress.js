import { InvalidAddressError, isUTXOAddress } from "@bigmi/core";
//#region src/actions/getAddress.ts
function getAddress(address) {
	if (!isUTXOAddress(address)) throw new InvalidAddressError({ address });
	return address;
}
//#endregion
export { getAddress };

//# sourceMappingURL=getAddress.js.map