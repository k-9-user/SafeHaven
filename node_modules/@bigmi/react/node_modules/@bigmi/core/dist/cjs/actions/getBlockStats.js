Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const require_errors_block = require("../errors/block.js");
//#region src/actions/getBlockStats.ts
async function getBlockStats(client, { blockHash, blockNumber, stats }) {
	const blockHashOrNumber = blockHash || blockNumber;
	if (!blockHashOrNumber) throw new require_errors_block.BlockNotFoundError({
		blockHash,
		blockNumber
	});
	try {
		const params = [blockHashOrNumber];
		if (stats) params.push(stats);
		return await client.request({
			method: "getblockstats",
			params
		}, { dedupe: true });
	} catch (_error) {
		throw new require_errors_block.BlockNotFoundError({
			blockHash,
			blockNumber
		});
	}
}
//#endregion
exports.getBlockStats = getBlockStats;

//# sourceMappingURL=getBlockStats.js.map