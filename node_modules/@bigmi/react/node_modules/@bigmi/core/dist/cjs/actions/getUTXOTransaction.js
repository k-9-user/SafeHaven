Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const require_errors_transaction = require("../errors/transaction.js");
//#region src/actions/getUTXOTransaction.ts
async function getUTXOTransaction(client, { txId, blockHash }) {
	try {
		const params = [txId, true];
		if (blockHash) params.push(blockHash);
		return await client.request({
			method: "getrawtransaction",
			params
		});
	} catch (_error) {
		throw new require_errors_transaction.TransactionNotFoundError({
			blockHash,
			hash: txId
		});
	}
}
//#endregion
exports.getUTXOTransaction = getUTXOTransaction;

//# sourceMappingURL=getUTXOTransaction.js.map