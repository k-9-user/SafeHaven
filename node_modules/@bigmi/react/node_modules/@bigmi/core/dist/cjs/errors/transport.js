Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const require_errors_base = require("./base.js");
//#region src/errors/transport.ts
var UrlRequiredError = class extends require_errors_base.BaseError {
	constructor() {
		super("No URL was provided to the Transport. Please provide a valid RPC URL to the Transport.", { name: "UrlRequiredError" });
	}
};
var TransportMethodNotSupportedError = class extends require_errors_base.BaseError {
	constructor({ method }) {
		super(`No transport found that supports the method: ${method}`, { name: "TransportMethodNotSupportedError" });
	}
};
var AllTransportsFailedError = class extends require_errors_base.BaseError {
	constructor({ method, params, errors, totalAttempts }) {
		const errorMessages = errors.map(({ transport, error, attempt }) => `${attempt}. ${transport}: ${error.message}`).join("\n");
		super(`All ${totalAttempts} transports failed for method "${method}": \n${errorMessages}`, {
			name: "AllTransportsFailedError",
			metaMessages: [
				`Method: ${method}`,
				`Total attempts: ${totalAttempts}`,
				`Failed transports: ${errors.map((e) => e.transport).join(", ")}`
			]
		});
		this.method = method;
		this.params = params;
		this.errors = errors;
		this.totalAttempts = totalAttempts;
	}
};
//#endregion
exports.AllTransportsFailedError = AllTransportsFailedError;
exports.TransportMethodNotSupportedError = TransportMethodNotSupportedError;
exports.UrlRequiredError = UrlRequiredError;

//# sourceMappingURL=transport.js.map