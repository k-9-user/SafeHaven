Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const require_utils_uid = require("../utils/uid.js");
const require_utils_buildRequest = require("../utils/buildRequest.js");
//#region src/factories/createTransport.ts
/**
* @description Creates an transport intended to be used with a client.
*/
function createTransport({ key, methods, name, request, retryCount = 3, retryDelay = 150, timeout, type }, value) {
	const uid$1 = require_utils_uid.uid();
	return {
		config: {
			key,
			methods,
			name,
			request,
			retryCount,
			retryDelay,
			timeout,
			type
		},
		request: require_utils_buildRequest.buildRequest(request, {
			methods,
			retryCount,
			retryDelay,
			uid: uid$1
		}),
		value
	};
}
//#endregion
exports.createTransport = createTransport;

//# sourceMappingURL=createTransport.js.map