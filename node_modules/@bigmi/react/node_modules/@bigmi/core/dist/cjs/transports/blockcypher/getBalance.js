Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const require_utils_url = require("../../utils/url.js");
const require_transports_blockcypher_utils = require("./utils.js");
//#region src/transports/blockcypher/getBalance.ts
const getBalance = async (client, { baseUrl, apiKey }, { address }) => {
	const apiUrl = require_utils_url.urlWithParams(`${baseUrl}/addrs/${address}`, { token: apiKey });
	const response = await client.request({
		url: apiUrl,
		fetchOptions: { method: "GET" }
	});
	if (response.error) return { error: {
		code: require_transports_blockcypher_utils.getRpcErrorCode(response.error),
		message: response.error
	} };
	return { result: BigInt(response.balance) };
};
//#endregion
exports.getBalance = getBalance;

//# sourceMappingURL=getBalance.js.map