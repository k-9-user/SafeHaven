Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const require_utils_url = require("../../utils/url.js");
const require_transports_blockcypher_utils = require("./utils.js");
//#region src/transports/blockcypher/getTransactionFee.ts
const getTransactionFee = async (client, { baseUrl, apiKey }, { txId }) => {
	const apiUrl = require_utils_url.urlWithParams(`${baseUrl}/txs/${txId}`, { token: apiKey });
	const response = await client.request({
		url: apiUrl,
		fetchOptions: { method: "GET" }
	});
	if (response.error) return { error: {
		code: require_transports_blockcypher_utils.getRpcErrorCode(response.error),
		message: response.error
	} };
	return { result: BigInt(response.fees) };
};
//#endregion
exports.getTransactionFee = getTransactionFee;

//# sourceMappingURL=getTransactionFee.js.map