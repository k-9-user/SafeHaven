Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const require_utils_stringify = require("../utils/stringify.js");
const require_errors_request = require("../errors/request.js");
const require_utils_idCache = require("../utils/idCache.js");
const require_utils_withTimeout = require("../utils/withTimeout.js");
//#region src/transports/getHttpRpcClient.ts
function getHttpRpcClient(url, options = {}) {
	return { async request(params) {
		const { body, onRequest = options.onRequest, onResponse = options.onResponse, timeout = options.timeout ?? 1e4 } = params;
		const fetchOptions = {
			...options.fetchOptions ?? {},
			...params.fetchOptions ?? {}
		};
		const { headers, method, signal: signal_ } = fetchOptions;
		try {
			const response = await require_utils_withTimeout.withTimeout(async ({ signal }) => {
				const preparedBody = body ? Array.isArray(body) ? body.map((b) => ({
					jsonrpc: "2.0",
					id: b.id ?? require_utils_idCache.idCache.take(),
					...b
				})) : {
					jsonrpc: "2.0",
					id: body.id ?? require_utils_idCache.idCache.take(),
					...body
				} : void 0;
				const init = {
					...fetchOptions,
					body: preparedBody ? require_utils_stringify.stringify(preparedBody) : void 0,
					headers: {
						...method !== "GET" ? { "Content-Type": "application/json" } : void 0,
						...headers
					},
					method: method || "POST",
					signal: signal_ || (timeout > 0 ? signal : null)
				};
				const request = new Request(params.url ?? url, init);
				if (onRequest) await onRequest(request, init);
				return await fetch(params.url ?? url, init);
			}, {
				errorInstance: new require_errors_request.TimeoutError({
					body: body ?? {},
					url: params.url ?? url,
					timeout
				}),
				timeout,
				signal: true
			});
			if (onResponse) await onResponse(response);
			let data;
			if (response.headers.get("Content-Type")?.startsWith("application/json")) data = await response.json();
			else {
				data = await response.text();
				data = JSON.parse(data || "{}");
			}
			if (!response.ok) throw new require_errors_request.HttpRequestError({
				body,
				details: require_utils_stringify.stringify(data.error) || response.statusText,
				headers: response.headers,
				status: response.status,
				url
			});
			return data;
		} catch (err) {
			if (err instanceof require_errors_request.HttpRequestError) throw err;
			if (err instanceof require_errors_request.TimeoutError) throw err;
			throw new require_errors_request.HttpRequestError({
				body,
				cause: err,
				url
			});
		}
	} };
}
//#endregion
exports.getHttpRpcClient = getHttpRpcClient;

//# sourceMappingURL=getHttpRpcClient.js.map