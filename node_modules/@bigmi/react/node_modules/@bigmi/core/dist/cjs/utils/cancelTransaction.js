Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
require("../_virtual/_rolldown/runtime.js");
let bitcoinjs_lib = require("bitcoinjs-lib");
//#region src/utils/cancelTransaction.ts
function cancelTransaction(psbt, accountAddress) {
	const newPsbt = new bitcoinjs_lib.Psbt();
	const inputs = psbt.data.inputs;
	const txInputs = psbt.txInputs;
	for (let i = 0; i < inputs.length; i++) {
		const input = inputs[i];
		const txInput = txInputs[i];
		const inputData = {
			hash: txInput.hash,
			index: txInput.index
		};
		if (input.nonWitnessUtxo) inputData.nonWitnessUtxo = input.nonWitnessUtxo;
		else if (input.witnessUtxo) inputData.witnessUtxo = input.witnessUtxo;
		else throw new Error("Input UTXO information is missing");
		if (input.redeemScript) inputData.redeemScript = input.redeemScript;
		if (input.witnessScript) inputData.witnessScript = input.witnessScript;
		newPsbt.addInput(inputData);
	}
	const outputs = psbt.txOutputs;
	let totalOutputValue = BigInt(0);
	for (const output of outputs) totalOutputValue += output.value;
	if (totalOutputValue <= BigInt(0)) throw new Error("Total output value must be greater than zero");
	const outputScript = bitcoinjs_lib.address.toOutputScript(accountAddress);
	newPsbt.addOutput({
		script: outputScript,
		value: totalOutputValue
	});
	newPsbt.txInputs.forEach((_, index) => {
		newPsbt.setInputSequence(index, 4294967293);
	});
	return newPsbt;
}
//#endregion
exports.cancelTransaction = cancelTransaction;

//# sourceMappingURL=cancelTransaction.js.map