Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const require_runtime = require("../_virtual/_rolldown/runtime.js");
const require_types_chain = require("../types/chain.js");
const require_types_address = require("../types/address.js");
let _noble_hashes_sha256 = require("@noble/hashes/sha256");
let bech32 = require("bech32");
let bs58 = require("bs58");
bs58 = require_runtime.__toESM(bs58);
//#region src/utils/getAddressInfo.ts
const addressTypes = {
	0: {
		type: require_types_address.AddressType.p2pkh,
		network: require_types_chain.Network.Mainnet
	},
	111: {
		type: require_types_address.AddressType.p2pkh,
		network: require_types_chain.Network.Testnet
	},
	5: {
		type: require_types_address.AddressType.p2sh,
		network: require_types_chain.Network.Mainnet
	},
	196: {
		type: require_types_address.AddressType.p2sh,
		network: require_types_chain.Network.Testnet
	}
};
const parseBech32 = (address) => {
	let decoded;
	try {
		if (address.startsWith("bc1p") || address.startsWith("tb1p") || address.startsWith("bcrt1p")) decoded = bech32.bech32m.decode(address);
		else decoded = bech32.bech32.decode(address);
	} catch (_error) {
		throw new Error("Invalid address");
	}
	const network = {
		bc: require_types_chain.Network.Mainnet,
		tb: require_types_chain.Network.Testnet,
		bcrt: require_types_chain.Network.Regtest
	}[decoded.prefix];
	if (network === void 0) throw new Error("Invalid address");
	const witnessVersion = decoded.words[0];
	if (witnessVersion < 0 || witnessVersion > 16) throw new Error("Invalid address");
	const data = bech32.bech32.fromWords(decoded.words.slice(1));
	let type;
	if (data.length === 20) type = require_types_address.AddressType.p2wpkh;
	else if (witnessVersion === 1) type = require_types_address.AddressType.p2tr;
	else type = require_types_address.AddressType.p2wsh;
	const purpose = getAddressPurpose(type);
	return {
		bech32: true,
		network,
		address,
		type,
		purpose
	};
};
const getAddressInfo = (address) => {
	let decoded;
	const prefix = address.substring(0, 2).toLowerCase();
	if (prefix === "bc" || prefix === "tb") return parseBech32(address);
	try {
		decoded = bs58.default.decode(address);
	} catch (_error) {
		throw new Error("Invalid address");
	}
	const { length } = decoded;
	if (length !== 25) throw new Error("Invalid address");
	const version = decoded[0];
	const checksum = decoded.slice(length - 4, length);
	const expectedChecksum = (0, _noble_hashes_sha256.sha256)((0, _noble_hashes_sha256.sha256)(decoded.slice(0, length - 4))).slice(0, 4);
	if (checksum.some((value, index) => value !== expectedChecksum[index])) throw new Error("Invalid address");
	if (!Object.keys(addressTypes).map(Number).includes(version)) throw new Error("Invalid address");
	const addressType = addressTypes[version];
	return {
		...addressType,
		address,
		bech32: false,
		purpose: getAddressPurpose(addressType.type)
	};
};
const getAddressPurpose = (type) => {
	switch (type) {
		case require_types_address.AddressType.p2tr: return "ordinals";
		case require_types_address.AddressType.p2wsh: return "stacks";
		case require_types_address.AddressType.p2wpkh:
		case require_types_address.AddressType.p2sh:
		case require_types_address.AddressType.p2pkh: return "payment";
		default: throw new Error("Invalid address type");
	}
};
const getAddressChainId = (address) => {
	switch (getAddressInfo(address).network) {
		case require_types_chain.Network.Mainnet: return require_types_chain.ChainId.BITCOIN_MAINNET;
		case require_types_chain.Network.Testnet: return require_types_chain.ChainId.BITCOIN_TESTNET;
		case require_types_chain.Network.Regtest: return require_types_chain.ChainId.BITCOIN_SIGNET;
		default: return require_types_chain.ChainId.BITCOIN_MAINNET;
	}
};
//#endregion
exports.getAddressChainId = getAddressChainId;
exports.getAddressInfo = getAddressInfo;

//# sourceMappingURL=getAddressInfo.js.map