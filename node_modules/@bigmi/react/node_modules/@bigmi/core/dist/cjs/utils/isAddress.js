Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const require_utils_getAddressInfo = require("./getAddressInfo.js");
//#region src/utils/isAddress.ts
const isAddress = (address, network) => {
	try {
		const addressInfo = require_utils_getAddressInfo.getAddressInfo(address);
		if (network) return network === addressInfo.network;
		return true;
	} catch (_error) {
		return false;
	}
};
//#endregion
exports.isAddress = isAddress;

//# sourceMappingURL=isAddress.js.map