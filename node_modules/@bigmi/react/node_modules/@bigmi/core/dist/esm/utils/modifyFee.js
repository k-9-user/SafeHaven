import { Psbt, address } from "bitcoinjs-lib";
//#region src/utils/modifyFee.ts
function modifyFee(psbt, newFee, accountAddress) {
	const newPsbt = new Psbt();
	const inputs = psbt.data.inputs;
	const outputs = psbt.txOutputs;
	for (let i = 0; i < inputs.length; i++) {
		const input = inputs[i];
		const inputData = {
			hash: psbt.txInputs[i].hash,
			index: psbt.txInputs[i].index
		};
		if (input.nonWitnessUtxo) inputData.nonWitnessUtxo = input.nonWitnessUtxo;
		else if (input.witnessUtxo) inputData.witnessUtxo = input.witnessUtxo;
		if (input.redeemScript) inputData.redeemScript = input.redeemScript;
		if (input.witnessScript) inputData.witnessScript = input.witnessScript;
		newPsbt.addInput(inputData);
	}
	const changeOutputScript = address.toOutputScript(accountAddress);
	for (const output of outputs) {
		const outputData = {
			script: output.script,
			value: output.value
		};
		if (output.script.length === changeOutputScript.length && output.script.every((value, index) => value === changeOutputScript[index])) {
			outputData.value = output.value - newFee;
			if (outputData.value < 0) throw new Error("Insufficient funds to adjust the fee by the specified amount.");
		}
		newPsbt.addOutput(outputData);
	}
	newPsbt.txInputs.forEach((_, index) => {
		newPsbt.setInputSequence(index, 4294967293);
	});
	return newPsbt;
}
//#endregion
export { modifyFee };

//# sourceMappingURL=modifyFee.js.map