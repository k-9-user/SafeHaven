import type { Client } from 'viem';
export declare function isBatchingSupported({ client, chainId, skipReady, }: {
    client?: Client;
    chainId: number;
    skipReady?: boolean;
}): Promise<boolean>;
//# sourceMappingURL=isBatchingSupported.d.ts.map