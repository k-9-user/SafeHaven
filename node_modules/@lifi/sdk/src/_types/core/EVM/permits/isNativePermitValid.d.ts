import type { SignedTypedData, TypedData } from '@lifi/types';
/**
 * Checks if an existing native permit is valid for the given requirements
 */
export declare const isNativePermitValid: (permit: SignedTypedData, typedData: TypedData) => boolean;
//# sourceMappingURL=isNativePermitValid.d.ts.map