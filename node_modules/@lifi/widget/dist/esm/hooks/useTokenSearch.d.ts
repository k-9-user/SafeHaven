import { type TokenExtended } from '@lifi/sdk';
import type { FormType } from '../stores/form/types.js';
export declare const useTokenSearch: (chainId?: number, tokenQuery?: string, enabled?: boolean, formType?: FormType) => {
    token: TokenExtended | undefined;
    isLoading: boolean;
};
