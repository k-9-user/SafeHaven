import type { FormType } from '../stores/form/types.js';
export declare const useTokens: (formType?: FormType, search?: string, chainId?: number) => {
    allTokens: {
        [chainId: number]: import("@lifi/sdk").TokenExtended[];
    } | undefined;
    isLoading: boolean;
    isSearchLoading: boolean;
};
