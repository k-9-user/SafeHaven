{
  "name": "safehaven-money",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"BACKEND,FRONTEND\" --prefix-colors \"cyan,magenta\" \"npm run dev --prefix agent\" \"npm run dev --prefix projet\"",
    "build": "npm run build --prefix agent && npm run build --prefix projet",
    "install:all": "npm install --prefix agent && npm install --prefix projet"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
